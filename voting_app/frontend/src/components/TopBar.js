import React from 'react';

const TopBar = () => (
    <div className="topBar-wrapper">
        <h2 className="ui green ribbon label app-label"><span>Sentio - a Voting App</span></h2>
        <div className="ui divider"/>
    </div>
);

export default TopBar;
